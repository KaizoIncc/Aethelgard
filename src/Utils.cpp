#include "Utils.hpp"

// -----------------------------------------------------------------------------------
// ---------------------------- Utils Implementation ---------------------------------
// -----------------------------------------------------------------------------------

// Este archivo ahora está COMPLETAMENTE VACÍO porque todas las funciones
// están implementadas como inline en el header.

// ¡No más implementaciones complejas!
// ¡No más gestión manual de memoria!
// ¡No más cachés que mantener!

// Si en el futuro necesitas funciones especializadas que combinen
// múltiples operaciones, las implementarías aquí.

// Ejemplo de función futura que podría ir aquí:
/*
vector<uint8_t> CryptoUtils::createSignedTransaction(
    const string& privateKey, 
    const Transaction& tx) {
    
    // Serializar transacción
    auto txData = tx.serialize();
    
    // Firmar
    auto signature = signMessage(privateKey, txData);
    
    // Crear transacción firmada
    SignedTransaction signedTx(tx, signature);
    return signedTx.serialize();
}
*/